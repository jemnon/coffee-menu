import React from 'react';

const CoffeeCounter = ({ items }) => {
  return (
    <section className="coffee-counter">
      <h1>Coffee Counter</h1>
    </section>
  );
};

export default CoffeeCounter;
